overwrite: true
schema: 'http://localhost:3000/graphql'
documents: null
generates:
  src/ts-types/generated.ts:
    config:
      noNamespaces: true
    plugins:
      - typescript-common
      - typescript-server
      - typescript-resolvers
